#include "walk.h"

#include <cstdlib>// Header file needed to use rand
#include <ctime> // Header file needed to use time

int main(){
    using namespace std;
	using Vector::Vector;
    Vector step;
    Vector result;
    double nowLen;
    double lenStep;
    double angle;

    srand(time(0));
}